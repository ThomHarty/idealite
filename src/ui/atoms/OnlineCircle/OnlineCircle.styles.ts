import {COLORS} from '../../../theme';
import {OnlineCircleStyles} from './OnlineCircle.types';

export const baseStyles: OnlineCircleStyles = {
  circle: {
    height: 18,
    width: 18,
    borderRadius: 9,
    borderWidth: 1,
    borderColor: COLORS.WHITE,
  },
};
