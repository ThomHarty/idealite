import { StyleSheet } from "react-native";

import { LinearBackgroundStyles } from "./LinearBackground.types";

export const baseStyles: LinearBackgroundStyles = StyleSheet.create({
    container: {
        flex: 1,
        overflow: "hidden",
    }
});
