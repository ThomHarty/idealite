import { ActiveCircleStyles } from "./ActiveCircle.types";

export const baseStyles: ActiveCircleStyles = {
    circle: {
        height: 10,
        width: 10,
        borderRadius: 5,
        borderWidth: 1,
    },
};
