import {ThemeKeys, COLORS} from '../../../../theme';

// default
const color: ThemeKeys = {
  light: COLORS.GREY_DARK,
  dark: COLORS.GREY_DARK,
};

// active
const activeColor: ThemeKeys = {
  light: COLORS.PRIMARY,
  dark: COLORS.PRIMARY,
};

export const colors = {
  color,
  activeColor,
};
