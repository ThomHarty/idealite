import { TextStyle, ViewStyle } from "react-native";

export interface IFeedInstructionsCardProps {

};

export interface FeedInstructionsCardStyles {
    container?: ViewStyle;
    halfContainer?: ViewStyle;
    dividerContainer?: ViewStyle;
    divider?: ViewStyle;
    iconContainer?: ViewStyle;
    headingContainer?: TextStyle;
};
