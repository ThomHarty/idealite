import { ViewStyle } from "react-native";

export interface ILikesHeaderProps {
    heading: string;
    subHeading?: string;
};

export interface LikesHeaderStyles {
    container?: ViewStyle;
    headingContainer?: ViewStyle;
    subHeadingContainer?: ViewStyle;
};