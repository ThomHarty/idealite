import { AccountTemplateStyles } from "./AccountTemplate.types";

export const useStyles = () => {
    const baseStyles: AccountTemplateStyles = {
        container: {
            flex: 1,
        },
    };

    return baseStyles;
};
